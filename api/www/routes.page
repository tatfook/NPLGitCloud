<?npl
include_once("./root/routes.page");
include_once("./emojis/routes.page");
include_once("./gists/routes.page");

local apis = {
    check_root_api,
    check_emojis_api,
    check_gists_api
};

for i = 1, #apis do
    if (apis[i]()) then
        break;
    end
end
?>
